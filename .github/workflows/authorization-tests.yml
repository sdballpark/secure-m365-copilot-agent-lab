name: Authorization Security Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  security-invariants:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run authorization and gateway tests
        run: pytest -q tests/test_authorization.py tests/test_gateway.py

      - name: Run corpus schema tests
        run: pytest -q tests/test_corpus_schema.py
